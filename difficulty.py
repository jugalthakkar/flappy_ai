from enum import Enum, auto

# Define an Enum
class Difficulty(Enum):
    # VERY_EASY = auto()  # auto() assigns unique, auto-incrementing integer values
    EASY = auto()
    MEDIUM = auto()
    # HARD = auto()
    # VERY_HARD = auto()